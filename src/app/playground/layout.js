export const metadata = {
    title: 'Playground | Tiptap-Max Editor',
    description: 'Try Tiptap-Max editor in action. Experiment with rich text editing, tables, code blocks, images, and more. Free forever, no account needed.',
    robots: {
        index: false,
        follow: true,
    },
};

export default function PlaygroundLayout({ children }) {
    return children;
}
